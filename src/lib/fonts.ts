import { Gluten, Inter, Unbounded } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const unbounded = Unbounded({ subsets: ["latin"] });
export const cus = Gluten({ subsets: ["latin"] });